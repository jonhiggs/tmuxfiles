#!/usr/bin/env bash
TMUX_WINDOW=9
tmux new-window -t ${TMUX_WINDOW} -n tmux -- bash --noprofile --norc
tmux send-keys -t:${TMUX_WINDOW} C-z "cd $1; tig status; nvr --remote-send ':GitGutterAll<CR>'; exit" Enter
tmux select-window -t:${TMUX_WINDOW}
