#!/usr/bin/env bash
source "$(dirname "$0")/../lib/bargraph.sh"

function cpu_used() {
  top -b -n2 -p1 | awk '/^%Cpu/ { s=$8 } END { print 100 - int(s) }'
}

used=$(cpu_used)
color='#[fg=colour015]'

printf "C%s%s#[fg=colour015]" ${color} "$(bargraph ${used})"
